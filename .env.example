# Environment Variables for Cloudflare Workers Deployment

# Required for production deployment
# Set these in your Cloudflare Workers dashboard or wrangler.toml

# API Keys for External Services (Optional)
ALPHA_VANTAGE_API_KEY="your_alpha_vantage_key"
COINGECKO_API_KEY="your_coingecko_key"

# Application Configuration
ENVIRONMENT="production"

# Optional: Monitoring and Analytics
LOG_LEVEL="info"

# Database: Cloudflare D1 (configured in wrangler.toml)
# KV Storage: Cloudflare KV (configured in wrangler.toml)
# No need for DATABASE_URL, REDIS_URL, JWT_SECRET, etc. with Cloudflare Workers